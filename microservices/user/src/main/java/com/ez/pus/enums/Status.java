package com.ez.pus.enums;


/**
 * This enum represents the status of a user.
 * Each enum constant represents a specific status: offline, online, sleep, or working.
 */
public enum Status {
    OFFLINE,
    ONLINE,
    SLEEP,
    WORKING;
}

package com.ez.pus.enums;

/**
 * This enum represents the types of notifications.
 * Each enum constant represents a specific type of notification.
 */
public enum NotificationType {
    FOLLOW_REQUEST,
    POST_SHARE,
    POST_LIKE,
    STORY_LIKE,
    STORY_REPLY,
    COMMENT_REPLY,
    COMMENT,
    COMMENT_LIKE,
    MESSAGE,
    GROUP_MESSAGE,
}

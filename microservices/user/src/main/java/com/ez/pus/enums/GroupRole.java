package com.ez.pus.enums;

/**
 * This enum represents the roles in a group.
 * Each enum constant represents a specific role: an admin or a member.
 */
public enum GroupRole {
    ADMIN,
    MEMBER
}

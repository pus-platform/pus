package com.ez.pus.enums;

/**
 * This enum represents the different views in the application.
 * Each enum constant represents a specific view: community, user, or public.
 */
public enum View {
    COMMUNITY,
    USER,
    PUBLIC
}
